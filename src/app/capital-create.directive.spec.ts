import { CapitalCreateDirective } from './capital-create.directive';

describe('CapitalCreateDirective', () => {
  it('should create an instance', () => {
    const directive = new CapitalCreateDirective();
    expect(directive).toBeTruthy();
  });
});
