export interface User {
    name:string,
    mobile:number,
    isIndian:boolean
}

  // Create Custom Interface ( After this Step 
  // Open parent component ( and apply rupee in vehicle cost field ))

